import React from 'react';
import { Text, View, StyleSheet, TouchableOpacity, Image, FlatList } from 'react-native';


function Time() {
    return (
        <View>
            <Text>Time on Instagram</Text>
        </View>
    )
}

export default Time;
const styles = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: "column",
        backgroundColor: "white"
    }
})
